<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.fitness_application">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_gym"
            android:label="@string/app_name"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            android:name="com.orm.SugarApp">

        <meta-data android:name="DATABASE" android:value="sugar_example.db"/>
        <meta-data android:name="VERSION" android:value="1"/>
        <meta-data android:name="QUERY_LOG" android:value="true"/>
        <meta-data android:name="DOMAIN_PACKAGE_NAME" android:value="com.example"/>

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name="ActivityTimer"/>

        <activity android:name="ActivityGym"/>

        <activity android:name="ActivityCalc"/>

        <activity android:name="ActivityDiary"/>

        <activity android:name="ActivityStr"/>

        <activity android:name="ActivityWeight"/>

        <activity android:name="ActivityBeach"/>

        <activity android:name=".ActivityStopwatch"/>

        <activity android:name=".ActivityNewExercise"/>

    </application>

</manifest>